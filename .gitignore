# Secrets — never commit. See CLAUDE.md hard constraints.
.env
.env.*
!.env.example
!.env.production.example
*.pem
*.key
/secrets/
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local model weights / scratch media
/models/
/media/
.DS_Store

# Machine-local Claude Code permission state
.claude/settings.local.json

# Hosting linkage from the abandoned OpenAI Sites attempt. The privacy site is
# published from the gh-pages branch instead.
.openai/

# Dashboard build artefacts
dashboard/node_modules/
dashboard/dist/

# Ingested media. Personal correspondence — same rule as .env and /secrets.
# The default CATCHMENT_BLOB_ROOT points here when running outside Docker.
/data/
dashboard/tsconfig.tsbuildinfo
